{block menu}
<div class="navbar">
	<div class="navbar-inner">
		<div class="container">
			<ul class="nav">
						{foreach $navig as $category}
						<li class=" {ifCurrent :Front:Category:show $category->id,$category->webname}active{/ifCurrent}" >
							<a n:href=":Front:Category:show $category->id,$category->webname " >{_ $category->name}</a>
						</li>
						{/foreach}
			</ul>
		</div>
	</div>
</div>
{/block}
