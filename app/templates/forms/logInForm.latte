{form form class => "form-log-in form-horizontal" }

{*{var $target = $form->getFormMsgTarget()}*}
{include "$tplDir/page/flashes.latte", flashes => $form->errors, target => ''}

<div class="control-group">
	{label login class => "control-label"}{/label}
	<div class="controls">{input login}</div>
</div>
<div class="control-group">
	{label pass class => "control-label"}{/label}
	<div class="controls">{input pass}</div>
</div>
<div class="control-group">
	<div class="controls">{input signup class => "btn btn-lg ajax-btn" }</div>
</div>


{/form}
